import { PermissionDefinition } from "@vendure/core"

export const ReadAnalytics = new PermissionDefinition({
    name: "ReadAnalytics",
    description: "Read visitor analytics and product view statistics"
})
